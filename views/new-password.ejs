<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Password</title>
    <link rel="stylesheet" href="/style/output.css">
    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon">
</head>

<body class="bg-gray-900 text-gray-200">
    <div class="min-h-screen flex flex-col justify-start items-center pt-10 pb-28 px-5">
        <div class="w-full max-w-xl flex justify-end mb-4">
            <a href="/" class="underline text-white font-semibold hover:text-blue-500">Home</a>
        </div>
        <h1 class="text-3xl font-bold mb-8 text-slate-200">New Password</h1>

        <!-- Message -->
        <% if (message) { %>
            <div class="bg-blue-500 text-white px-2 py-1 rounded mb-6 break-words">
                <%= message %>
            </div>
        <% } %>

        <!-- Form -->
        <form action="/reset-password/<%= token %>" method="POST" class="w-full max-w-lg">
            <div class="mb-6">
                <label for="password" class="block mb-2">New Password:</label>
                <input type="password" id="password" name="password" placeholder="Enter your new password"
                    class="w-full px-3 py-2 bg-gray-800 rounded-md focus:outline-none focus:ring focus:ring-blue-500 text-gray-200"
                    required>
            </div>
            <button type="submit"
                class="bg-blue-500 hover:bg-blue-600 px-4 py-2 rounded-md text-white font-semibold">Reset
                Password</button>
        </form>
    </div>
</body>

</html>
