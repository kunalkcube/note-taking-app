<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shared Note</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/remixicon/4.2.0/remixicon.css" integrity="sha512-OQDNdI5rpnZ0BRhhJc+btbbtnxaj+LdQFeh0V9/igiEPDiWE2fG+ZsXl0JEH+bjXKPJ3zcXqNyP4/F/NegVdZg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/style/output.css">
    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon">
</head>
<body class="bg-gray-900 text-gray-200">
    <div class="min-h-screen flex flex-col justify-start items-center pt-10 pb-28 px-5">
        <h1 class="text-3xl font-bold mb-8 text-slate-200">Shared Note</h1>
        <div class="max-w-2xl w-full">
            <h2 class="text-xl font-semibold mb-4"><%= note.title %></h2>
            <p class="text-gray-300 mb-8"><%= note.content %></p>
            
            <% if (permission === 'editable') { %>
                <h2 class="text-2xl font-bold mb-8 text-slate-200">Edit Note</h2>
                <form action="/note/update" method="POST">
                    <input type="hidden" name="noteId" value="<%= note._id %>">
                    <input type="text" name="title" value="<%= note.title %>" class="w-full px-3 py-2 mb-4 bg-gray-800 rounded-md focus:outline-none focus:ring focus:ring-blue-500 text-gray-200" required>
                    <textarea name="content" class="w-full px-3 py-2 bg-gray-800 rounded-md focus:outline-none focus:ring focus:ring-blue-500 text-gray-200 resize-none h-40 sm:h-64" required><%= note.content %></textarea>
                    <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded mt-4">Update Note</button>
                </form>
            <% } %>
        </div>
    </div>
</body>
</html>
