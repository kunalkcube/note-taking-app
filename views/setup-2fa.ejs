<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Setup Two-Factor Authentication</title>
    <link rel="stylesheet" href="/style/output.css">
    <link rel="shortcut icon" href="/favicon.png" type="image/x-icon">
</head>

<body class="bg-gray-900 text-gray-200">
    <div class="min-h-screen flex flex-col items-center justify-start p-5">
        <div class="w-full max-w-xl flex justify-end mb-4">
            <a href="/" class="underline text-white font-semibold hover:text-blue-500">Home</a>
        </div>
        <h1 class="text-4xl font-bold mb-10">Setup Two-Factor Authentication</h1>
        <div class="p-6 rounded-lg w-full max-w-xl">
            <p class="mb-6 text-lg text-gray-300">Scan the QR code below with your authenticator app or paste the secret key:</p>
            <div class="flex justify-center mb-6">
                <img src="<%= qrCode %>" alt="QR Code" class="rounded-lg border-2 border-gray-700 shadow-lg">
            </div>
            <div class="mb-6">
                <label for="secret" class="block text-sm font-medium text-gray-300 mb-2">Secret Key</label>
                <p id="secret" class="bg-gray-700 text-gray-200 p-2 rounded-md text-center break-words"><%= secret %></p>
            </div>
            <form action="/profile/verify-2fa" method="POST" class="space-y-6">
                <input type="hidden" name="secret" value="<%= secret %>">
                <div>
                    <label for="token" class="block text-sm font-medium text-gray-300 mb-2">Enter the token from your
                        app</label>
                    <input type="text" name="token" id="token" placeholder="Enter token"
                        class="w-full px-4 py-2 bg-gray-700 border border-gray-600 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 text-gray-200"
                        required>
                </div>
                <div class="flex justify-end">
                    <button type="submit"
                        class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-lg shadow-md transition duration-300 ease-in-out">Verify</button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>